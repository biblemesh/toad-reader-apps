/**
 * This file is intentionally left blank.
 *
 * Purpose:
 * - This file overrides Expo’s default service worker output.
 * - The project does NOT use service workers, so we provide an empty override.
 * - Keeping this file prevents Expo from injecting its own service worker.
 *
 * Safe to remove if we later decide to re-enable service workers.
 */
